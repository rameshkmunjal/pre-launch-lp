const Header=()=>{
    return(
        <header className="header">
            <img src={'./assets/logo.svg'} alt={"logo pre launch lp"} />            
        </header>
    )
}

export default Header;